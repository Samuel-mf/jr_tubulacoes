services:

  app:
    build: .
    restart: always
    environment:
      NODE_ENV: production
      PORT: 3000
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: rjtubulacao
      DB_PASS: 6Q2&22ndBH$h
      DB_NAME: rjtubulacao
    depends_on:
      - db

  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: rjtubulacao
      POSTGRES_PASSWORD: 6Q2&22ndBH$h
      POSTGRES_DB: rjtubulacao
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  postgres_data:
